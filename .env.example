# Frontend-only Vite vars.
# When Electron loads the app from file:// and these stay empty,
# the frontend now falls back to http://localhost:5000 automatically.
VITE_BACKEND_URL=

# Optional explicit Socket.IO origin. Defaults to VITE_BACKEND_URL or the detected frontend origin.
VITE_SOCKET_URL=
